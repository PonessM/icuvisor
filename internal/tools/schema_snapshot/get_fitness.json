{
  "additionalProperties": false,
  "properties": {
    "athlete_id": {
      "description": "Target athlete; defaults to selected athlete in coach mode, or the only athlete otherwise. Format: i12345 or 12345.",
      "type": "string"
    },
    "end_date": {
      "description": "Inclusive upper bound for upstream weekly bucket anchor dates, as athlete-local YYYY-MM-DD.",
      "type": "string"
    },
    "include_full": {
      "default": false,
      "description": "When true, include raw upstream weekly rows selected by anchor date; exact partial-week values remain unavailable.",
      "type": "boolean"
    },
    "include_per_sport_load_trends": {
      "default": false,
      "description": "When true, fetch an 84-day weekly-summary lookback and return explicit unavailable metadata for per-sport CTL/ATL/TSB; weekly category load is not distributed into invented daily samples.",
      "type": "boolean"
    },
    "start_date": {
      "description": "Inclusive lower bound for upstream weekly bucket anchor dates, as athlete-local YYYY-MM-DD.",
      "type": "string"
    }
  },
  "required": [
    "start_date",
    "end_date"
  ],
  "type": "object"
}
